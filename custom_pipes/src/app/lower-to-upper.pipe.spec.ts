import { LowerToUpperPipe } from './lower-to-upper.pipe';

describe('LowerToUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new LowerToUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
